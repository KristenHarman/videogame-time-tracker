<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Videogame Time Tracker</title>
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
    <h1>Videogames</h1>
    <ul>
    <% games.forEach( el => { %>
            <li class='gameItem' data-id='<%=el._id%>'>
                <span class='<%= el.completed === true ? 'completed' : 'not'%>'><%= el.game %></span>
                <span class='del'> Delete </span>
            </li>
    <% }) %>    
    </ul>

    <h2><%= user.userName %> has <%= left %> game(s) left to finish.</h2>

    <form action="/games/createGame" method='POST'>
        <input type="text" placeholder="Enter Game" name='gameItem'>
        <input type="submit">
    </form>

    <h4>Health</h4>
    <progress id="health" value="100" max="100"></progress>
    <button class="decHealth">Missed a day</button>

    
  <!-- EXP Bar  -->
 <h4>EXP</h4>
  <progress id="exp" value="0" max="100"></progress>
 
 <h4>Game Progress</h4>
  <progress id="progress" value="0" max="100"></progress>


  <button class="oneHourIncExp">Played one hour</button>


    <a href="/logout">Logout</a>

    <script src="js/main.js"></script>
</body>
</html>